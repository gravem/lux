<div class="container">

<h1>New review</h1>
<%= simple_form_for [@booking, @review] do |f| %>
  <%= f.input :rating, collection: [1, 2, 3, 4, 5,], default:[1]%>
  <%= f.input :content %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
</div>
